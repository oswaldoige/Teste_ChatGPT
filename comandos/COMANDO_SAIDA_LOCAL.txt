Executar automacao revisor. Use apenas as skills locais do revisor-processual-tjrj espelhadas neste repositorio, ignore qualquer outra automacao/projeto, analise somente os arquivos `.json` indicados para a rodada, priorize os modelos da pasta `modelos`, complemente, se necessario, com `${GABINETE_ACERVO}`, e gere os provimentos finais em `.docx` na pasta fisica local `${GABINETE_SAIDA}`, evitando usar o OneDrive como destino principal.
